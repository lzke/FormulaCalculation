# FormulaCalculation

```
pip install -r requirements.txt
python main.py
```

```
# 示例: 公式 (c1 * v1 + c2 * v2) / (v1 + v2) = end
# 其中 c1 v1 c2 v2 v1 v2 需要确认数值或者范围, end 不需要确认范围
#
# 公式格式如下:
# formula: (c1 * v1 + c2 * v2) / (v1 + v2)
#
# 参数格式如下: 范围 精度
# parameter:
#   c1: { range: [0.1, 0.15], graduate: 0.001 }
#   v1: 0.01
#   c2: 5
#   v2: { range: [0.00021, 0.00088], graduate: 0.000001 }
```
